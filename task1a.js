
  var i="hello"
  switch(i){
    case "hi":
        console.log("string are equal to hi");
        break;
    case "hello":
        console.log("string are equal to  hello");
        break;
    default:
        console.log("the value of i is not match with any values");
        break;
  }
  function getRandomInt(max) {
    return Math.floor(Math.random() * Math.floor(max));
  }
  console.log(Math.random());

Math.floor(Math.random() * 6) + 1  
console.log(Math.random());
   